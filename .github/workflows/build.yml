name: Test GRUB Build

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-24.04

    steps:
      - name: Test GitHub Actions
        run: |
          echo "================================"
          echo "GRUB build environment is running"
          echo "================================"
          uname -a
          gcc --version
          make --version
